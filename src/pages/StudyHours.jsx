import { Clock3, Minus, Plus } from "lucide-react"
import { useStudy } from "../context/useStudy"

export default function StudyHours() {
  const { studyHours, setStudyHours } = useStudy()
  const adjust = (amount) => setStudyHours((current) => Math.max(0, Number((current + amount).toFixed(1))))
  return <div className="space-y-7"><section><p className="eyebrow">Time invested</p><h1 className="page-title">Study hours</h1><p className="page-subtitle">Keep a lightweight record of focused time. Your total is saved automatically.</p></section><div className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><section className="panel flex flex-col items-center justify-center py-12 text-center"><div className="rounded-3xl bg-sky-50 p-5 text-sky-600 dark:bg-sky-500/10"><Clock3 size={32} /></div><p className="mt-6 text-sm font-medium text-slate-500">Total study hours</p><p className="mt-2 text-6xl font-bold tracking-tight text-slate-950 dark:text-white">{studyHours}<span className="text-2xl text-slate-400">h</span></p><div className="mt-8 flex items-center gap-3"><button onClick={() => adjust(-0.5)} className="icon-button border" aria-label="Subtract half an hour"><Minus size={18} /></button><span className="w-20 text-sm text-slate-500">Adjust by 0.5h</span><button onClick={() => adjust(0.5)} className="icon-button border" aria-label="Add half an hour"><Plus size={18} /></button></div></section><section className="panel"><p className="eyebrow">Weekly focus</p><h2 className="section-title mt-1">Build a study rhythm</h2><p className="mt-3 text-sm leading-6 text-slate-500 dark:text-slate-400">A realistic weekly target keeps your momentum visible without making study feel like another chore.</p><div className="mt-8"><div className="flex justify-between text-sm"><span className="font-medium text-slate-700 dark:text-slate-200">Weekly goal</span><span className="text-slate-500">{studyHours} / 8 hours</span></div><div className="mt-3 h-3 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-sky-500 transition-all" style={{ width: `${Math.min(studyHours / 8 * 100, 100)}%` }} /></div><p className="mt-4 text-sm text-slate-500">{studyHours >= 8 ? "Weekly goal reached. Excellent work." : `${(8 - studyHours).toFixed(1)} hours left to reach your weekly goal.`}</p></div></section></div></div>
}
