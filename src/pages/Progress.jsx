import { CheckCircle2, Circle, Target, TrendingUp } from "lucide-react"
import { useStudy } from "../context/useStudy"

export default function Progress() {
  const { tasks, stats, studyHours } = useStudy()
  return <div className="space-y-7"><section><p className="eyebrow">Your momentum</p><h1 className="page-title">Progress</h1><p className="page-subtitle">A clear view of the work you have completed and what is next.</p></section><div className="grid gap-5 md:grid-cols-3"><div className="panel md:col-span-2"><div className="flex flex-col items-center gap-8 sm:flex-row"><div className="progress-ring progress-ring-large" style={{ "--progress": `${stats.progress * 3.6}deg` }}><div><strong>{stats.progress}%</strong><span>complete</span></div></div><div><p className="eyebrow">Overall completion</p><h2 className="mt-2 text-2xl font-bold text-slate-900 dark:text-white">You're {stats.progress}% done - keep going!</h2><p className="mt-3 max-w-md text-sm leading-6 text-slate-500 dark:text-slate-400">Every completed task builds momentum. Stay close to your priorities and protect time for focused study.</p></div></div></div><div className="panel flex flex-col justify-between"><div className="rounded-2xl bg-emerald-50 p-3 text-emerald-600 dark:bg-emerald-500/10"><TrendingUp size={20} /></div><div className="mt-7"><p className="text-sm text-slate-500">Study hours</p><p className="mt-1 text-3xl font-bold text-slate-900 dark:text-white">{studyHours}<span className="text-base font-medium text-slate-400">h</span></p></div></div></div><section className="panel"><div className="section-header"><div><p className="eyebrow">Task breakdown</p><h2 className="section-title">Your checklist</h2></div><div className="flex gap-4 text-sm"><span className="text-emerald-600">{stats.completed} done</span><span className="text-slate-500">{stats.pending} remaining</span></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{tasks.length ? tasks.map((task) => <div key={task.id} className="flex items-center gap-3 rounded-2xl bg-slate-50 p-4 dark:bg-slate-800/60">{task.completed ? <CheckCircle2 className="shrink-0 text-emerald-500" size={19} /> : <Circle className="shrink-0 text-slate-300" size={19} />}<span className={`truncate text-sm font-medium ${task.completed ? "text-slate-400 line-through" : "text-slate-700 dark:text-slate-200"}`}>{task.title}</span></div>) : <p className="py-5 text-sm text-slate-500">No tasks to measure yet.</p>}</div></section><div className="panel flex items-center gap-4"><div className="rounded-2xl bg-amber-50 p-3 text-amber-600 dark:bg-amber-500/10"><Target size={20} /></div><div><h2 className="font-semibold text-slate-900 dark:text-white">A simple goal for today</h2><p className="mt-1 text-sm text-slate-500">Complete one pending task before starting another.</p></div></div></div>
}
